/// Adds two numbers.
/// @param a {i64} First addend.
/// @param b {i64} Second addend.
/// @return {i64} The sum.
fn add(a, b) { a + b }

/// Greets a user.
/// @param name {string} A user's name.
/// @return {string} Greeting message.
fn greet(name) { `Hello, ${name}!` }

